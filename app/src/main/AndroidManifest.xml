<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.homcooked.homecooked">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Account_Creation" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Nearby_Foods" />
        <activity android:name=".view_food_details" />

        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".view_food_details"
            tools:ignore="DuplicateActivity"></activity>
        <activity android:name=".PostActivity"></activity>
        <activity android:name=".UsersPosts"></activity>
        <activity android:name=".DeletePost"></activity>

        <activity
            android:name=".view_food_details"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".ViewReviews"></activity>
    </application>

</manifest>